Objects:
  store:
    users:
      listOfUser
        User:
          name
          $
          create()
          addToTab($)
          removeFromTab($)
      listAllUsers(sortOrder)
    items:
      listOfItem
        Item:
          name
          $
          containsMilk
          create()
          purchase()
      listAllItems(sortOrder)
    sales:
      listOfSale
        sale:
          listOfItems
          user/cash
      makeSale()
      refund
  gui:
    buttons:
      listOfButton
        button:
          location
          item
      add()
      remove()
    inputs:
      listOfInput
        input: // look into json
  name
